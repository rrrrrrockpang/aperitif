// let toggle = false;

// chrome.runtime.onInstalled.addListener(() => {
//   chrome.storage.sync.set({ "toggle": toggle });
//   console.log('Default background color set to %cgreen', `color: ${toggle}`);
// });